export interface Theme {
  colors: {
    background: string,
    textColor: string,
    buttonBackground: string,
    buttonColor: string,
    widgetBackground: string,
    widgetBorderColor: string,
    widgetBoxShadow: string,
    logoFilter: string,
    tableBorderColor: string,
    divisionColor: string,
    tableIcon: string,
  }
}